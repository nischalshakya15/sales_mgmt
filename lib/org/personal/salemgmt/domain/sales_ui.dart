import 'package:flutter/material.dart';

class SaleUi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('SalesManagement')),
      body: Center(child: Text('Hello sales team')),
    );
  }
}
